package com.simulator.hospital;

import com.simulator.hospital.view.SimuView;

/**
 * The main entry point for the hospital simulation application.
 */
public class Main {

    /**
     * The main method that launches the simulation view.
     *
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        SimuView.launch(SimuView.class);
    }
}
